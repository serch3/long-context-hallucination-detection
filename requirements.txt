# Core libraries
numpy>=2.0.0
pandas>=2.1.0
scikit-learn>=1.5.0

# Hugging Face
datasets>=2.14.0
# pin tokenizers to a transformers-compatible range
# Use a transformers/tokenizers pair compatible with iTiger environment
# Pin transformers to a concrete 4.57.x release that accepts tokenizers 0.22.x
transformers==4.57.5  # fixed to avoid resolver backtracking
tokenizers>=0.22,<0.23
evaluate>=0.4.0             # compute_metrics for HF Trainer

# PyTorch (CUDA-enabled build — uses pytorch's CUDA 12.4 wheel index)
# If your cluster uses a different CUDA version, change cu124 → cu121 or cu118
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.2.0
torchvision>=0.19.0
torchaudio>=2.2.0

# Training utilities
accelerate>=1.0.0           # mixed-precision training on HPC cluster
sentencepiece>=0.2.0

# Config
pyyaml>=6.0.1

# Notebook & visualization
jupyterlab>=4.2.0
ipykernel>=6.29.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Environment
python-dotenv>=1.0.0
